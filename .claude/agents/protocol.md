# 워크플로우

## 기본 흐름 (모든 변경)

1. 수정 전: Grep/Glob으로 영향 범위 확인
2. 수정 실행 (PostToolUse 훅이 안티패턴 자동 검출)
3. 셀프리뷰: git diff 확인, 호출처 Grep, UI는 브라우저 확인
4. 보고

## advisor (Opus)

복잡한 아키텍처 결정, 트레이드오프 분석이 필요할 때만 호출.
일상적인 코드 수정에는 사용하지 않음.

## deployer

"릴리즈", "배포", "태그" 요청 시 호출.
