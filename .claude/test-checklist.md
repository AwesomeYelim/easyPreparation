# easyPreparation 브라우저 테스트 체크리스트

> 테스트 URL: `http://localhost:3000` (UI) / `http://localhost:8080` (Go API)
> 서버 시작: `make dev`

---

## A. 인증 / 초기 로딩

### A-1. 로그인
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 1 | 비로그인 상태 UI | 브라우저에서 `localhost:3000` 접속 | 로그인 아이콘 표시, 주요 기능 접근 제한 |
| 2 | Google 로그인 | 로그인 아이콘 클릭 | Google OAuth → 로그인 완료 → 프로필 이미지 표시 |
| 3 | 설정 자동 로드 | 로그인 직후 | SettingsLoader가 `/api/settings` 호출 → 테마/폰트 크기 즉시 적용 |
| 4 | 교회 정보 로드 | 로그인 직후 | `/api/user` 호출 → userInfoState에 교회명/email/figmaInfo 저장 |

### A-2. 로그아웃
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 5 | 로그아웃 | 사이드바 → "logout" 클릭 | 세션 종료, 로그인 화면으로 돌아감 |

---

## B. 네비게이션 바 (NavBar)

| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 6 | 탭 이동 | Bulletin / Lyrics / Bible 클릭 | 각 페이지로 라우팅, 활성 탭 하이라이트 |
| 7 | Display 버튼 | "Display" 버튼 클릭 | 제어판 열림 + `localhost:8080/display` 새 창 오픈 |
| 8 | Display 버튼 (재클릭) | 제어판 열린 상태에서 다시 클릭 | 제어판 닫힘, Display 창은 유지 |
| 9 | 프로필 버튼 | 우측 프로필 이미지 클릭 | 사이드바 슬라이드 인 |

---

## C. 주보 (Bulletin) 페이지

### C-1. 예배 순서 편집
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 10 | 예배 유형 변경 | 드롭다운에서 "주일예배" → "수요예배" | 순서 항목이 수요예배용으로 변경 |
| 11 | 항목 추가 | WorshipOrder에서 항목 태그 클릭 | SelectedOrder에 해당 항목 추가 |
| 12 | 항목 삭제 | SelectedOrder에서 항목의 X 클릭 | 확인 후 삭제 |
| 13 | 항목 드래그 | SelectedOrder에서 항목 드래그 | 순서 변경됨 |
| 14 | 항목 상세 편집 | SelectedOrder에서 항목 클릭 | Detail 패널에 편집 필드 표시 |

### C-2. 상세 편집 (Detail)
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 15 | 성경 구절 입력 | info가 `b_*`인 항목 → BibleSelect 컴포넌트 | 책/장/절 선택 UI, obj에 "45/8:1-3" 형식 저장 |
| 16 | 텍스트 입력 | info가 `edit`인 항목 → textarea | 자유 텍스트 입력 가능 |
| 17 | 인도자 입력 | info에 `edit` 포함 항목 | lead 필드에 이름 입력 가능 |
| 18 | 교회소식 편집 | info가 `notice`인 항목 | ChurchNews 컴포넌트 표시, 계층형 공지 편집 |
| 19 | 자동 항목 | info에 edit/notice 없는 항목 | "이 항목은 자동으로 처리됩니다" 표시 |

### C-3. 주보 생성
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 20 | Figma 미설정 시 | Figma key/token 없는 상태 | "예배 자료 생성하기" 버튼 비활성 |
| 21 | 주보 생성 | Figma 설정 후 "예배 자료 생성하기" 클릭 | 로딩 오버레이 + WS 진행 메시지 표시 |
| 22 | WS 진행 상황 | 생성 중 | 메시지 큐(400ms 간격)로 진행 상황 순차 표시, 최근 9개 로그 |
| 23 | 생성 완료 | WS `done` 메시지 수신 | 자동 다운로드 (`/download?target=파일명`) |
| 24 | 이력 기록 확인 | 생성 완료 후 사이드바 → "주보 생성 내역" | 방금 생성한 주보가 목록에 표시 (status: success) |

### C-4. Display 전송
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 25 | Display 전송 | "Display 전송" 클릭 | 로딩 오버레이 + `display_loading` WS 메시지 |
| 26 | 전처리 진행 | 찬송/성경 항목 포함 시 | "찬송 100 처리 중..." 등 진행 메시지 |
| 27 | 전처리 완료 | 전처리 끝나면 | 제어판 자동 열림 + Display 창에 첫 항목 표시 |
| 28 | Display 이력 | 전송 완료 후 사이드바 → "Display 생성 내역" | 전송 기록이 목록에 표시 |

---

## D. 가사 (Lyrics) 페이지

### D-1. 자유 곡 탭
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 29 | 곡 추가 | 제목 입력 → 추가 버튼 | 곡 목록에 추가, 기본 BPM = 설정의 default_bpm |
| 30 | 곡 펼치기 | 곡 좌측 화살표 클릭 | 가사 textarea + BPM 입력 + Dedup 버튼 표시 |
| 31 | 가사 입력 | textarea에 가사 입력 | 입력된 텍스트 Recoil 상태에 저장 |
| 32 | BPM 변경 | BPM 숫자 입력 변경 | 해당 곡의 BPM 값 업데이트 |
| 33 | 중복 제거 (Dedup) | "중복 제거" 버튼 클릭 | 반복 가사 → "(x2)" 표기로 압축, 성공 플래시 |
| 34 | 곡 삭제 | 곡 우측 삭제 버튼 | 목록에서 제거 |
| 35 | 가사 검색 | "전체 가사를 검색" 버튼 | 각 곡 제목으로 Google 검색 → 가사 자동 채움 |
| 36 | PDF 생성 | "가사 기반으로 PDF 생성" 클릭 | ZIP 다운로드 (곡명.zip 또는 "곡명 외 N.zip") |
| 37 | 가사 이력 | 생성 후 사이드바 → "가사 PPT 생성 내역" | 기록 표시 |

### D-2. 찬송가 검색 탭
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 38 | 번호 검색 | "100" 입력 → 검색 | 100장 검색 결과 표시 |
| 39 | 제목 검색 | "주 하나님" 입력 → 검색 | 해당 제목 포함 찬송가 목록 |
| 40 | 상세 보기 | 결과에서 찬송가 클릭 | 우측에 번호/제목/가사 표시 |
| 41 | Display 전송 | 상세에서 "Display 전송" 클릭 | Display에 찬송 이미지 추가 (append) |

### D-3. Display 전송 (가사 탭)
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 42 | 자유곡 Display 전송 | 자유곡 탭 → "Display 전송" | Display에 가사 항목 추가 (BPM 포함) |
| 43 | 기존 순서에 추가 | 이미 Display 순서 있는 상태에서 전송 | 기존 순서 뒤에 append (기존 항목 유지) |

---

## E. 성경 (Bible) 페이지

### E-1. 성경 조회
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 44 | 버전 기본값 | 페이지 로드 시 | 설정의 preferred_bible_version이 기본 선택 |
| 45 | 구약/신약 탭 | "구약" / "신약" 클릭 | 해당 성경 목록 표시 (구약: book_order ≤ 39) |
| 46 | 책 선택 | 목록에서 책 클릭 (예: 로마서) | 장 선택 그리드 표시 |
| 47 | 장 선택 | 장 번호 클릭 (예: 8장) | 해당 장의 구절 목록 표시 |
| 48 | 장 이동 | ← / → 네비게이션 버튼 | 이전/다음 장으로 이동 (경계에서 비활성) |

### E-2. 구절 선택
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 49 | 단일 선택 | 구절 클릭 | 해당 구절 하이라이트, 상단에 "로마서 8:1" 표시 |
| 50 | 범위 선택 | 1절 클릭 → 5절 Shift+클릭 | 1-5절 전체 선택, "로마서 8:1-5" 표시 |
| 51 | 범위 해제 | 모두 선택된 상태에서 Shift+클릭 | 범위 내 전체 선택 해제 |
| 52 | 비연속 선택 | 1절 클릭, 5절 클릭, 10절 클릭 | "로마서 8:1,5,10" 형태로 표시 |
| 53 | Display 전송 | 구절 선택 후 "Display 전송" 클릭 | Display에 성경 구절 추가 (append) |

### E-3. 검색
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 54 | 구절 검색 | "사랑" 입력 → Enter 또는 검색 클릭 | 검색 결과 카드 목록 표시 |
| 55 | 검색 결과 이동 | 검색 결과 카드 클릭 | 해당 책/장으로 이동, 구절 하이라이트 |

### E-4. 비교 모드
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 56 | 비교 모드 활성 | "비교" 버튼 클릭 | 2번째 버전 드롭다운 표시 |
| 57 | 비교 표시 | 비교 버전 선택 후 구절 로드 | 2컬럼 그리드 (좌: 주 버전, 우: 비교 버전) |
| 58 | 비교 모드 해제 | "비교" 버튼 다시 클릭 | 단일 컬럼으로 복구 |

---

## F. Display 제어판 (DisplayControlPanel)

### F-1. 기본 제어
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 59 | 항목 클릭 점프 | 제어판에서 항목 클릭 | Display 창이 해당 항목으로 즉시 이동 |
| 60 | 이전/다음 버튼 | ◀ / ▶ 클릭 | Display 슬라이드 이동 (서브페이지 우선) |
| 61 | 키보드 ← → | 키보드 좌/우 화살표 | 이전/다음 슬라이드 (input/textarea에서는 무시) |
| 62 | 현재 위치 동기화 | Display 창에서 직접 키보드로 이동 | 제어판의 활성 항목이 자동 업데이트 (WS position) |

### F-2. 드래그 & 삭제
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 63 | 항목 드래그 | 항목을 위/아래로 드래그 | 순서 변경 → Display에도 즉시 반영 |
| 64 | 항목 삭제 | 휴지통 아이콘 클릭 | 항목 제거 → Display에서도 삭제 |

### F-3. 서브 섹션
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 65 | 찬송 펼치기 | 찬송 항목 펼치기 | 페이지별 가사 미리보기 표시 (60자 truncate) |
| 66 | 서브 섹션 클릭 | 찬송 가사 미리보기 중 하나 클릭 | 해당 이미지 페이지로 바로 점프 |
| 67 | 성경 펼치기 | 성경 항목 펼치기 | 3절 단위 페이지 미리보기 |

### F-4. 타이머
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 68 | 타이머 활성화 | Enable 토글 | 카운트다운 시작 (MM:SS 표시) |
| 69 | 타이머 반복 | Repeat 버튼 | 현재 항목 끝나면 자동으로 다음 항목 이동 |
| 70 | 타이머 속도 | 속도 슬라이더 (0.5x ~ 2x) | 카운트다운 속도 변경 |
| 71 | 타이머 재시작 | Restart 버튼 | 현재 항목 타이머 처음부터 |

### F-5. OBS 상태
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 72 | OBS 연결됨 | config/obs.json 있고 OBS 실행 중 | 녹색 점 + "OBS {씬이름}" 표시 |
| 73 | OBS 미연결 | config/obs.json 없거나 OBS 꺼짐 | 빨간 점 + "OBS Disconnected" |
| 74 | 씬 전환 | 항목 이동 시 (찬송→말씀) | OBS 씬 자동 전환 (매핑된 경우만) |

---

## G. Display 화면 (localhost:8080/display)

### G-1. 항목별 렌더링
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 75 | 성경 본문 | 성경 항목으로 이동 | 구절 텍스트 표시, 3절 초과 시 서브페이지 분할 |
| 76 | 찬송 | 찬송 항목으로 이동 | 표지 (곡번호) + 악보 이미지 페이지 순서대로 |
| 77 | 성시교독 | 교독 항목으로 이동 | 표지 없이 이미지만 바로 표시 |
| 78 | 신앙고백 | 신앙고백 항목으로 이동 | 사도신경 전문 표시 (10줄 페이징) |
| 79 | 주기도문 | 주기도문 항목으로 이동 | 7줄 축약, 한 화면 표시 (페이지 분할 없음) |
| 80 | 대표기도 | 대표기도 항목으로 이동 | 기도자명 큰 텍스트 표시 |
| 81 | 참회의 기도 | 참회의 기도 항목으로 이동 | 기도문 좌정렬 표시 |
| 82 | 말씀 | 말씀 항목으로 이동 | 설교 제목 + 목사명 |
| 83 | 교회소식 | 교회소식 항목으로 이동 | 공지 텍스트 (계층 구조) |

### G-2. 배경 이미지
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 84 | 기본 배경 | 일반 항목 | Figma 배경 (`Frame 1.png`) + 어두운 오버레이 |
| 85 | 커스텀 배경 | 전주/찬양/참회의 기도 항목 | `tmp/{title}.png` 배경 + 교회명 박스 (우하단) |
| 86 | 교회명 박스 | 커스텀 배경 항목 | JacquesFrancois 폰트, 영문 교회명, opacity 박스 |

### G-3. 키보드/WS
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 87 | Display 키보드 | Display 창에서 ← → | 슬라이드 이동, 제어판에 위치 동기화 |
| 88 | WS 재연결 | Display 창 네트워크 끊김 후 복구 | 3초 후 자동 재연결, 마지막 위치 복원 |

---

## H. 가사 오버레이 (localhost:8080/display/overlay)

| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 89 | 오버레이 표시 | OBS Browser Source로 overlay 열기 | 투명 배경 + 가사 텍스트 (반투명 박스) |
| 90 | 찬송 가사 동기화 | Display에서 찬송 페이지 넘김 | overlay에 해당 페이지 가사 표시 (lyricsMap) |
| 91 | 성경 텍스트 | Display에서 성경 항목 | overlay에 구절 텍스트 표시 |
| 92 | 표지 페이지 | 찬송 표지 (곡번호 페이지) | overlay에 곡번호만 표시 |

---

## I. 사이드바

### I-1. 교회 정보
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 93 | 교회 정보 표시 | 사이드바 열기 | 교회명 + 영문명 표시 (미등록이면 "미등록") |
| 94 | 편집 모드 | "교회 정보" 영역 클릭 | input 필드 2개 (한글/영문) + 저장/취소 버튼 |
| 95 | 저장 | 교회명 입력 → "저장" 클릭 | POST /api/user → 성공 시 즉시 반영 |
| 96 | 취소 | "취소" 클릭 | 편집 전 값으로 복원 |

### I-2. 메뉴
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 97 | 설정 | "설정" 클릭 | SettingsPanel 모달 열림 |
| 98 | 라이선스 정보 | "라이선스 정보" 클릭 | (현재 미구현 — 클릭 반응 없음) |
| 99 | 주보 생성 내역 | 클릭 | HistoryList 모달 (type=bulletin 필터) |
| 100 | PPT 생성 내역 | 클릭 | HistoryList 모달 (type=ppt 필터) |
| 101 | 가사 PPT 생성 내역 | 클릭 | HistoryList 모달 (type=lyrics_ppt 필터) |
| 102 | Display 생성 내역 | 클릭 | HistoryList 모달 (type=display 필터) |

---

## J. 설정 패널 (SettingsPanel)

| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 103 | 성경 버전 변경 | 드롭다운 → "NIV" 선택 → 저장 | 성경 페이지에서 기본 버전이 NIV |
| 104 | 테마 변경 (Light) | 테마 → "light" → 저장 | 밝은 배경, 어두운 텍스트 |
| 105 | 테마 변경 (Dark) | 테마 → "dark" → 저장 | `data-theme="dark"` 적용, 어두운 배경 |
| 106 | 폰트 크기 | 20px 입력 → 저장 | 전체 UI 폰트 크기 즉시 커짐 (12~24 범위) |
| 107 | 기본 BPM | 120 입력 → 저장 | 가사 탭에서 새 곡 추가 시 BPM 기본값 = 120 |
| 108 | Display 레이아웃 | "wide" 선택 → 저장 | (현재 보류 — 저장만 됨) |
| 109 | 취소 | 값 변경 후 "취소" | 변경 없이 모달 닫힘 |
| 110 | 새로고침 유지 | 설정 저장 후 F5 | 설정값 그대로 유지 (서버에서 로드) |

---

## K. 생성 이력 (HistoryList)

| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 111 | 이력 로드 | 모달 열기 | `/api/history?email=...&type=bulletin` 호출 → 목록 표시 |
| 112 | 타입 뱃지 | 각 이력 항목 | 주보/PPT/가사PPT/Display 뱃지 색상 구분 |
| 113 | 날짜 표시 | 이력 항목 | 한국어 로케일 날짜 (toLocaleString('ko-KR')) |
| 114 | 상태 표시 | 성공/실패 항목 | 녹색(success) / 빨간색(failed) |
| 115 | 페이지네이션 | 20개 초과 이력 | 이전/다음 버튼으로 페이지 이동 |
| 116 | 빈 상태 | 이력 없는 타입 | "생성 내역이 없습니다" 표시 |

---

## L. z-index 계층 테스트

| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 117 | 로딩 < 제어판 | Display 전송 중 (로딩+제어판 동시) | 제어판(10000)이 로딩 오버레이(5000) **위** |
| 118 | 제어판 < 사이드바 | 제어판 열고 → 프로필 클릭 | 사이드바(10500)가 제어판(10000) **위** |
| 119 | 사이드바 < 모달 | 사이드바에서 "설정" 클릭 | 설정 모달(11000)이 사이드바(10500) **위** |
| 120 | 전체 순서 | 모두 동시 활성 | 모달 > 사이드바 > 제어판 > 로딩 > 일반 |
| 121 | 오버레이 클릭 | 사이드바 배경 오버레이 클릭 | 사이드바 닫힘 |

---

## M. 서버 상태 / 복구

### M-1. Display 상태 영속화
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 122 | 상태 저장 | Display 전송 후 서버 재시작 (`make dev`) | 이전 순서/위치 복원 (data/display_state.json) |
| 123 | 제어판 복원 | 페이지 새로고침 (F5) | GlobalDisplayPanel이 서버에서 상태 로드 → 제어판 자동 열림 |
| 124 | Display 창 복원 | Display 창 닫고 다시 열기 | 마지막 위치에서 시작 (WS로 현재 order+idx 수신) |

### M-2. WebSocket
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 125 | WS 재연결 | 서버 재시작 | UI가 자동 재연결 → 상태 복원 |
| 126 | Keepalive | 장시간 대기 | 50초마다 keepalive 메시지 (연결 유지) |

---

## N. 에러 / 엣지 케이스

| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 127 | 빈 순서 Display 전송 | 항목 0개 상태에서 Display 전송 | 에러 메시지 또는 아무 동작 안 함 |
| 128 | 잘못된 성경 범위 | 존재하지 않는 장/절 | 빈 결과 또는 에러 메시지 |
| 129 | 중복 곡 추가 (가사) | 같은 제목 곡 2번 추가 | 중복 검증 → 추가 거부 |
| 130 | PDF 없는 찬송 | DB에 has_pdf=false인 번호 | 이미지 없이 가사만 표시 |
| 131 | Google Drive 미연결 | config/auth.json 없음 | 찬송 이미지 생성 실패 → 빈 images, 가사만 표시 |
| 132 | 긴 텍스트 입력 | 교회소식에 매우 긴 텍스트 | 오버플로우 없이 스크롤 또는 줄바꿈 |
| 133 | 동시 사용자 | 2개 브라우저에서 동시 접속 | WS broadcast로 양쪽 제어판 동기화 |
| 134 | 네트워크 끊김 | Wi-Fi 끊기 → 복구 | WS 재연결 + API 재시도 가능 |

---

## O. 반응형 / 모바일

| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 135 | 768px 이하 제어판 | 브라우저 너비 줄이기 | 제어판 전체 너비 (100vw) |
| 136 | 768px 이하 padding | 제어판 열린 상태 | body padding-right 제거 (0) |
| 137 | 모바일 사이드바 | 모바일 너비에서 사이드바 | 320px 고정, 화면 안에 표시 |
| 138 | 모바일 모달 | 모바일에서 설정 모달 | max-width: 90vw, 넘침 없음 |
| 139 | 터치 타겟 | 480px 이하에서 버튼 크기 | 최소 44px 터치 영역 |

---

## P. 스케줄러 + 자동 스트리밍

### P-1. 스케줄 설정
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 140 | 모달 열기 | 사이드바 → 설정 → "스트리밍 스케줄" | 스케줄 설정 모달 열림 |
| 141 | 기본값 확인 | 모달 열기 | 주일(일 11:00), 오후(일 14:00), 수요(수 19:30), 금요(금 20:30) 4개 |
| 142 | 시간 변경 | 시간 input 변경 → 저장 | 새로고침 후에도 유지 |
| 143 | 활성/비활성 | 체크박스 토글 → 저장 | 비활성 스케줄은 카운트다운/실행 안 됨 |
| 144 | 카운트다운 분 | 5 → 10으로 변경 → 저장 | 10분 전부터 카운트다운 시작 |
| 145 | OBS 자동 스트리밍 | ON/OFF 토글 → 저장 | ON이면 예배 시작 시 스트리밍 자동 시작 |

### P-2. 스케줄 테스트 (⏱ / ▶ 버튼)
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 146 | 카운트다운 테스트 | ⏱ 버튼 클릭 | "10초 카운트다운 시작" alert |
| 147 | 카운트다운 배너 | ⏱ 테스트 후 | 제어판 상단에 빨간 배너 "주일예배 00:10" 카운트다운 |
| 148 | Display 카운트다운 | ⏱ 테스트 후 | `localhost:8080/display`에 검은 오버레이 + 큰 숫자 |
| 149 | 오버레이 카운트다운 | ⏱ 테스트 후 | `localhost:8080/display/overlay`에도 카운트다운 |
| 150 | 카운트다운 종료 | 10초 후 | 카운트다운 자동 사라짐 |
| 151 | 즉시 실행 테스트 | ▶ 버튼 클릭 | "스케줄 즉시 실행" alert |
| 152 | 순서 로드 | ▶ 테스트 후 | config/{worshipType}.json 순서가 Display에 로드 |
| 153 | 시작 알림 | ▶ 테스트 후 | 제어판에 schedule_started 메시지 수신 |

### P-3. OBS 스트리밍 제어
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 154 | 상태 표시 | OBS 연결 시 | 제어판 상단 "LIVE" 뱃지 또는 "방송 시작" 버튼 |
| 155 | 방송 시작 | "방송 시작" 클릭 | OBS 스트리밍 시작, "LIVE" 뱃지 표시 |
| 156 | 방송 종료 | "방송 종료" 클릭 | OBS 스트리밍 종료, 뱃지 사라짐 |
| 157 | OBS 미연결 | OBS 없이 버튼 클릭 | 에러 없이 무시 (no-op) |

### P-4. 실제 시간 테스트
| # | 테스트 | 조작 | 기대 결과 |
|---|--------|------|-----------|
| 158 | 실시간 카운트다운 | 시간을 현재+2분으로 설정 → 저장 | 카운트다운분 전부터 매초 브로드캐스트 |
| 159 | 실시간 자동 실행 | 설정 시간 도달 | 예배 순서 자동 로드 + OBS 스트리밍 시작(ON일 때) |
| 160 | 중복 방지 | 같은 시간 2번 | 한 번만 실행됨 (lastExecuted 플래그) |

### P-5. API 직접 테스트 (터미널)
```bash
# 스케줄 조회
curl http://localhost:8080/api/schedule

# 카운트다운 테스트
curl -X POST http://localhost:8080/api/schedule/test \
  -H 'Content-Type: application/json' \
  -d '{"action":"countdown","worshipType":"main_worship"}'

# 즉시 실행 테스트
curl -X POST http://localhost:8080/api/schedule/test \
  -H 'Content-Type: application/json' \
  -d '{"action":"trigger","worshipType":"main_worship"}'

# 스트리밍 상태
curl -X POST http://localhost:8080/api/schedule/stream \
  -H 'Content-Type: application/json' \
  -d '{"action":"status"}'
```

---

## Q. 외부 의존성 (선택 테스트)

> 아래 항목은 Google Drive / Ghostscript / OBS 등 외부 환경에 의존합니다.

| # | 테스트 | 전제 조건 | 기대 결과 |
|---|--------|-----------|-----------|
| 161 | 찬송 PDF → PNG | Ghostscript 설치 (`/opt/homebrew/bin/gs`) | Display에 악보 이미지 표시 |
| 162 | Google Drive 다운로드 | config/auth.json 설정 | 찬송/교독 PDF 자동 다운로드 |
| 163 | OBS 씬 전환 | OBS 실행 + config/obs.json | 항목 변경 시 OBS 씬 자동 전환 |
| 164 | Figma 이미지 | Figma key/token 설정 | 배경 이미지 다운로드 + 캐시 |

---

## 테스트 우선순위

| 우선순위 | 범위 | 항목 수 |
|---------|------|--------|
| P0 (필수) | A, C-3~C-4, F-1, G-1, L, P-2 | 인증, 주보생성, Display 제어, 렌더링, z-index, **스케줄 테스트** |
| P1 (중요) | B, D, E-1~E-2, I, J, K, M, P-1, P-3 | 네비게이션, 가사, 성경, 사이드바, 설정, 이력, 상태복구, **스케줄 설정/스트리밍** |
| P2 (보통) | C-1~C-2, E-3~E-4, F-2~F-5, H, P-4 | 순서 편집, 비교모드, 드래그, 타이머, 오버레이, 실시간 테스트 |
| P3 (낮음) | N, O, Q | 엣지케이스, 반응형, 외부 의존성 |
